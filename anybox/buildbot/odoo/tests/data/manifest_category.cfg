[ready]
buildout = standalone buildouts/6.1.cfg
build-category = mature
# no build-for: the builds will not be dispatching accross, e.g, pg
# versions, but if they have steps involving pg, they will have no way
# to skip them either (NOT-USED could help for that).

[wip]
buildout = standalone buildouts/6.1.cfg
build-category = unstable
